#include <stdio.h>
int fact();
int main()
{
    int n,ans;
    printf("enter the number:");
    scanf("%d",&n);
    ans=fact(n);
 printf("%d",ans);
}
int fact(int n)
{
    int fact=1;
    while(n>0)
    {
        fact=fact*n;
        n--;
    }
    return(fact);
}
